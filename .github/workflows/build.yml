name: Build

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Use HTTPS for public GitHub dependencies
        run: git config --global url."https://github.com/".insteadOf "git@github.com:"

      - name: Build MacGesture
        run: |
          xcodebuild \
            -project MacGesture.xcodeproj \
            -scheme MacGesture \
            -configuration Debug \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Sign application for local testing
        run: |
          codesign \
            --force --deep --sign - --timestamp=none \
            "$RUNNER_TEMP/DerivedData/Build/Products/Debug/MacGesture.app"
          codesign \
            --verify --deep --strict --verbose=2 \
            "$RUNNER_TEMP/DerivedData/Build/Products/Debug/MacGesture.app"

      - name: Package application
        run: |
          ditto \
            -c -k --sequesterRsrc --keepParent \
            "$RUNNER_TEMP/DerivedData/Build/Products/Debug/MacGesture.app" \
            "$RUNNER_TEMP/MacGesture-debug.zip"

      - name: Upload application
        uses: actions/upload-artifact@v7
        with:
          name: MacGesture-debug
          path: ${{ runner.temp }}/MacGesture-debug.zip
          if-no-files-found: error
